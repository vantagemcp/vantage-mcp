[project]
name = "vantage-mcp"
version = "1.3.0"
description = "Vantage - MCP server exposing AI-answer-engine visibility/citation checks (ChatGPT, Google AI Overview) as callable tools for AI agents and IDEs."
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.2.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/vantage_mcp"]
